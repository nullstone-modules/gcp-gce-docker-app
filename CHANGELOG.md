# 0.1.0 (Unreleased)

* Add `cloud_init_stanzas` output with COS-safe Docker bootstrap (tmpfs secrets, plain `docker run`, systemd)
* Add variables for image, ports, app env, secret names, host keys, and data directory
